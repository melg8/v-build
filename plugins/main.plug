# common build plugin, must be loaded everytime

# example
#
# --begin
# type = plugin_name
# name = bin | script
# dir = your_command_name ( 1 word )
# exec = path to executable from "plugins" dir
# args = real name of executable file
# desc = description, what does it do?
# --end

--begin
type = bin
name = print_help_msg
dir = plugins/system
exec = libtext_lib.so.1.0.0
args = void
desc = text input/output support
--end
