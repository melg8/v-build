#   example
#
#   --begin
#   type = bin | script
#   command = function | script
#   exec = path from system dir to exec file
#   ret_val = void || int || bool || char* || text (script)
#   args = arg1, arg2, argN || no
#   desc = description
#   --end

--begin
type = bin
command = export_variables
exec = system/libs/libos_lib.so.1.0.0
ret_val = void
args = no
desc = export variables for building os
--end
